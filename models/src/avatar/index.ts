/**
 * @interface Avatar
 * @description interface modeling meaningful info data retrieved by firebase storage about the avatars
 * @author Davide Ghiotto
 */
export interface Avatar {
	fullPath: string;
	url: string;
}
